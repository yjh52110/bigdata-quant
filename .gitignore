# OS
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
**/__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
dist/
dist-ssr/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo

# Secrets & credentials
*.env
.env.*
!*.env.example
!*.env.docker.example
*.key
*.pem
credentials*.json
backend/data/credentials.json
backend/data/encryption.key

# Databases
*.db
*.db-journal

# Ingested market/chain data (regenerate with /api/ingest)
backend/data/market/
backend/data/hypersync_output/
backend/data/*.jsonl
backend/data/compaction_state.json
backend/data/mcp_users.json
backend/data/jobs.db*
backend/data/chain/
.claude/

# Kaggle job artefacts fetched by kernels output: measurement payloads and
# parquet, not source. One benchmark file is 200 MB of random bytes, which
# GitHub rejects outright (100 MB limit).
backend/data/kaggle_output/
backend/data/kaggle_jobs.json

# Catalog: holds Drive folder ids and per-account placement. Not a credential,
# but it describes private storage layout and is machine-specific state, not source.
backend/data/_catalog/
